using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoterController : Gimicks
{
    //[SerializeField] int holesize = 4;
    //[SerializeField] GameObject myItem = null;

    /*
    public override DTText InteractGimick()
    {
        base.InteractGimick();
        ItemStack itemStack = GameObject.FindObjectOfType<ItemStack>();
        GameObject obj = Instantiate(myItem, transform.position, Quaternion.identity);
        Items i = obj.GetComponent<Items>();
        Debug.Log(i);
        itemStack.AddItem(i);
        base.DisableGimick();
        return null;
    }
    */
}
